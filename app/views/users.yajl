json = {
  count: count,
  page: page,
  size: size,
  users: users.map do |user|
    {
      id: user.id,
      email: user.email,
      nickname: user.nickname,
      avatar: user.avatar,
      is_admin: user.admin?,
    }
  end
}
